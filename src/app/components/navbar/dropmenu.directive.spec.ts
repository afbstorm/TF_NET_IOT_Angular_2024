import { DropmenuDirective } from './dropmenu.directive';

describe('DropmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DropmenuDirective();
    expect(directive).toBeTruthy();
  });
});
